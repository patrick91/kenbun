[project]
name = "fixture-api"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "pydantic-settings>=2.7",
    "uvicorn[standard]>=0.34",
]

[tool.fastapi]
entrypoint = "fixture_api.main:app"
