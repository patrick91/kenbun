[project]
name = "platform-api"
requires-python = ">=3.12"
dependencies = [
    "cross-inertia>=2.0",
    "fastapi>=0.115",
    "pydantic-settings>=2.7",
    "uvicorn[standard]>=0.34",
]

[tool.fastapi]
entrypoint = "platform_api.main:app"
