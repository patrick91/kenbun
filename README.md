# kenbun

`kenbun` is currently a minimal Python extension built with Rust and PyO3.

## Usage

```python
import kenbun

print(kenbun.hello())
```

This prints:

```text
Hello, world!
```

## Development

Run the tests with:

```bash
uv run pytest
```
